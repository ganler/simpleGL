#version 410 core

layout (location = 0) in vec3 coord3d;
layout (location = 1) in vec2 st_coord;

uniform mat4 mvp;
out vec2 text_coord;

void main(void)
{
   gl_Position = mvp * vec4(coord3d, 1.0);
   text_coord = st_coord;
}